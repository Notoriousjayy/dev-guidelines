# C Style Guide

## Formatting
- Use 4 spaces per indentation level; avoid tabs.
- Place braces `{}` on the same line for control structures:
  ```c
  if (condition) {
      // code
  } else {
      // code
  }
